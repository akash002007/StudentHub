import React from "react";
import { EmptyState } from "@/components/admin/common";

export default function AdminInternshipsPage() {
  return (
    <EmptyState
      title="Internship moderation workspace"
      description="Use this area to monitor internship quality, policy flags, and listing integrity."
    />
  );
}
