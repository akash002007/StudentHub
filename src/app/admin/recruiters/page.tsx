import React from "react";
import { EmptyState } from "@/components/admin/common";

export default function AdminRecruitersPage() {
  return (
    <EmptyState
      title="Recruiter management is ready for integration"
      description="This section is prepared for recruiter verification operations and compliance workflows."
    />
  );
}
